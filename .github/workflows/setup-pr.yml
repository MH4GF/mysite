name: ci

on:
  pull_request:
    branches:
      - main

jobs:
  depcruise:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332 # v4
      - uses: MH4GF/dependency-cruiser-report-action@2a2b37774bfe0b6749ee2bb589161865772fc172 # v2.4.2
        with:
          package-manager: pnpm
